# 基本設計書

**システム名**: mail-dispatcher（メール配信管理システム）
**バージョン**: 1.3
**作成日**: 2026-03-02
**更新日**: 2026-03-30

---

> ### この文書について
>
> **「基本設計書」とは？**
> 要件定義書で「何を作るか」が決まったら、次は「どのように作るか」の大枠を決める文書です。
> システムの骨格（技術スタック・画面構成・データ設計・API 構成）をまとめています。
>
> **研修での位置づけ**
> 実装を始める前にこの文書を読んで、「どんな技術を使うか」「どんな画面があるか」「データはどう管理されているか」を把握してください。

---

## 1. システム構成

### 1.1 アーキテクチャ概要

> **「アーキテクチャ」とは？**
> システムの「骨組み」のことです。どんな部品をどのように組み合わせてシステムを作るかの設計図です。

**システムの構成要素と流れ**

```
[クライアント] ブラウザ（Chrome / Edge など）
      |
      | HTTP(S) リクエスト / レスポンス
      v
[サーバー] Next.js アプリ
      |
      +-- Server Components  : ページの HTML を生成。DB に直接アクセス
      +-- Client Components  : ブラウザ側で動く UI（ボタン・フォームなど）
      +-- Server Actions     : フォーム送信処理。ページ遷移（リダイレクト）も担当
      +-- API Routes         : /api/... の URL で呼び出せる REST API
      |
      | Prisma ORM（SQL を書かずに DB を操作）
      v
[データ層] SQLite データベース（prisma/dev.db）
      ファイル 1 つで完結する DB。研修環境向け。

      |
      | Nodemailer（メール送信ライブラリ）
      v
[メール送信]
      +-- console モード: ターミナルに出力するだけ（実際には送信しない）← 研修はこちら
      +-- smtp モード  : SMTP サーバーに実際に送信する
```

> **用語解説**
> - **Server Components**: サーバー側で HTML を生成するコンポーネント。初期表示が速い
> - **Client Components**: ブラウザ側で動くコンポーネント。ボタンのクリック処理などを担当
> - **Server Actions**: フォーム送信などをサーバー側で処理する Next.js の機能
> - **API Routes**: `/api/...` の URL で呼び出せるサーバー処理。JSON でデータをやり取りする
> - **Prisma ORM**: SQL を書かずに TypeScript で DB 操作できるライブラリ

### 1.2 技術スタック

| 区分 | 採用技術 | バージョン | 役割 |
|------|----------|-----------|------|
| フレームワーク | Next.js (App Router) | 16.1.6 | Web アプリの全体的な骨格 |
| 言語 | TypeScript | 5.x | JavaScript に型安全性を追加 |
| ORM | Prisma | 7.x | SQL を書かずに DB 操作 |
| DB | SQLite | — | 研修用シンプル DB（ファイルベース） |
| メール送信 | Nodemailer | 7.x | メール送信ライブラリ |
| スタイル | Tailwind CSS v4 | — | ユーティリティファーストの CSS |
| ランタイム | Node.js | 20.x | JavaScript をサーバーで動かす実行環境 |
| バリデーション | Zod | — | 入力値の型・形式チェック |

### 1.3 ディレクトリ構成

```
mail-dispatcher/
|
+-- prisma/
|   +-- schema.prisma      テーブル定義（DB の設計図）
|   +-- dev.db             SQLite データベースファイル
|   +-- migrations/        DB の変更履歴
|   +-- seed.ts            初期データ投入スクリプト
|
+-- src/
|   +-- app/
|   |   +-- api/
|   |   |   +-- auth/          ログイン・ログアウト・パスワード
|   |   |   +-- admin/         管理者専用（ユーザーパスワード設定）
|   |   |   +-- contacts/      連絡先 CRUD + CSV import/export
|   |   |   +-- groups/        グループ CRUD
|   |   |   +-- invitations/   招待リンク発行・受諾
|   |   |
|   |   +-- dashboard/
|   |   |   +-- page.tsx       ダッシュボードトップ
|   |   |   +-- users/         ユーザー管理（ADMIN のみ）
|   |   |   +-- invite/        ユーザー招待
|   |   |   +-- contacts/      連絡先管理
|   |   |   +-- templates/     メールテンプレート管理
|   |   |   +-- campaigns/     配信履歴
|   |   |   +-- compose/       メール作成・送信
|   |   |
|   |   +-- login/             ログイン画面
|   |   +-- styles/            カスタム CSS
|   |   +-- globals.css        グローバルスタイル
|   |   +-- layout.tsx         全ページ共通レイアウト
|   |
|   +-- lib/
|       +-- auth/              認証関連（セッション・パスワード）
|       +-- email/             メール送信（コンソール/SMTP 切り替え）
|       +-- schemas/           Zod バリデーションスキーマ
|       +-- ui/                UI スタイルオブジェクト
|       +-- prisma.ts          Prisma クライアントのシングルトン
|       +-- scheduler.ts       予約送信のポーリング処理
|       +-- mail.ts            Magic Link メール送信
|
+-- docs/                      設計ドキュメント
+-- CLAUDE.md                  開発ガイドライン
+-- package.json               プロジェクト設定・依存パッケージ
```

---

## 2. 画面構成

---

### ⚠️ 画面デザインについて（重要）

> この設計書では **各画面に必要な機能（何ができるか）** を定義します。
>
> **画面のデザイン（見た目・レイアウト・配色・フォント・余白など）は実装者（あなた）に自由に任せます。**
>
> 以下の点を守れば、デザインは自由に設計してください：
>
> 1. **機能の充足**: 各画面の「機能要件」に記載されたすべての機能が動作すること
> 2. **ロール制御**: ADMIN / EDITOR / VIEWER のロールによる表示・操作の制御が正しく機能すること
> 3. **スタイルルール**: `CLAUDE.md` に記載されたボタン・フォーム・テーブルのスタイルクラスに従うこと

---

### 2.1 画面一覧

| 画面 ID | パス | 画面名 | アクセス可能ロール |
|---------|------|--------|-----------------|
| SCR-01 | `/login` | ログイン | 未認証ユーザー |
| SCR-02 | `/dashboard` | ダッシュボードトップ | 全ロール |
| SCR-03 | `/dashboard/users` | ユーザー管理 | ADMIN のみ |
| SCR-04 | `/dashboard/invite` | ユーザー招待 | ADMIN・EDITOR |
| SCR-05 | `/dashboard/contacts` | 連絡先一覧 | 全ロール |
| SCR-06 | `/dashboard/contacts/new` | 連絡先新規作成 | ADMIN・EDITOR |
| SCR-07 | `/dashboard/contacts/[id]` | 連絡先詳細・編集 | 全ロール（編集は ADMIN・EDITOR） |
| SCR-08 | `/dashboard/templates` | テンプレート一覧 | 全ロール |
| SCR-09 | `/dashboard/templates/new` | テンプレート新規作成 | ADMIN・EDITOR |
| SCR-10 | `/dashboard/templates/[id]/edit` | テンプレート編集 | ADMIN・EDITOR |
| SCR-11 | `/dashboard/campaigns` | 配信履歴一覧 | 全ロール |
| SCR-12 | `/dashboard/campaigns/[id]` | 配信履歴詳細 | 全ロール |
| SCR-13 | `/dashboard/compose` | メール作成・送信 | ADMIN・EDITOR |
| SCR-14 | `/dashboard/compose/result` | 送信結果 | ADMIN・EDITOR |

---

### 2.2 各画面の機能要件

> デザインは自由ですが、以下の機能は必ず実装してください。

---

#### SCR-01 ログイン画面（`/login`）

**概要**: 未認証ユーザーがシステムにログインする画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | パスワードログイン | メールアドレス＋パスワードを入力してログインできる |
| 2 | Magic Link ログイン | メールアドレスを入力して Magic Link を送信できる |
| 3 | エラー表示 | パスワード不一致・未設定などのエラーをユーザーに表示する |
| 4 | ログイン済みリダイレクト | すでにログイン済みの場合はダッシュボードへ自動遷移する |

---

#### SCR-02 ダッシュボードトップ（`/dashboard`）

**概要**: ログイン後のトップ画面。各機能への入口。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | ナビゲーション | 連絡先・テンプレート・配信履歴・ユーザー管理へのリンクを表示する |
| 2 | ログインユーザー情報 | 現在ログイン中のユーザー名・ロールを表示する |
| 3 | ログアウト | ログアウトボタンを押すとログイン画面へ遷移する |
| 4 | パスワード変更 | 自分のパスワードを変更できる |
| 5 | ロール別表示制御 | VIEWER の場合はユーザー管理リンクを非表示にする |

---

#### SCR-03 ユーザー管理（`/dashboard/users`）

**概要**: 組織のメンバーを管理する画面。ADMIN のみアクセス可。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | メンバー一覧表示 | メールアドレス・名前・ロール・パスワード設定状況を一覧表示する |
| 2 | パスワード設定 | 各メンバーのパスワードを設定・変更できる |
| 3 | アクセス制限 | ADMIN 以外がアクセスするとダッシュボードにリダイレクトされる |

---

#### SCR-04 ユーザー招待（`/dashboard/invite`）

**概要**: 新しいメンバーを組織に招待する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | 招待フォーム | 招待先メールアドレスと付与するロール（VIEWER / EDITOR）を入力できる |
| 2 | 招待リンク発行 | 送信後、ターミナルに招待 URL が出力される（研修環境） |
| 3 | 完了フィードバック | 招待リンク発行成功・失敗のメッセージを表示する |

---

#### SCR-05 連絡先一覧（`/dashboard/contacts`）

**概要**: 配信先の連絡先を一覧表示・管理する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | 連絡先一覧表示 | 登録順にページネーション表示（50件/ページ） |
| 2 | キーワード検索 | 氏名・会社名・メール・電話・メモで検索できる |
| 3 | グループ絞り込み | グループを選択して連絡先を絞り込める |
| 4 | 連絡先選択 | チェックボックスで複数の連絡先を選択できる |
| 5 | メール作成遷移 | 選択した連絡先をもとにメール作成画面へ遷移できる |
| 6 | 新規作成ボタン | 連絡先新規作成画面へ遷移するボタン（EDITOR 以上のみ有効） |
| 7 | CSV エクスポート | 全連絡先を CSV ファイルでダウンロードできる |
| 8 | CSV インポート | CSV ファイルから連絡先を一括登録できる（EDITOR 以上） |
| 9 | 削除確認 | 削除ボタン押下時に確認ダイアログを表示する（EDITOR 以上） |

---

#### SCR-06 連絡先新規作成（`/dashboard/contacts/new`）

**概要**: 新しい連絡先を登録する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | 入力フォーム | 氏名（必須）・メール・電話・会社名・メモを入力できる |
| 2 | グループ選択 | 既存グループを複数選択して連絡先に設定できる |
| 3 | グループ新規作成 | その場で新しいグループを作成して選択できる |
| 4 | バリデーション | 氏名未入力・メール重複などのエラーをリアルタイム表示する |
| 5 | 保存 | 保存後、連絡先一覧へ戻る |

---

#### SCR-07 連絡先詳細・編集（`/dashboard/contacts/[id]`）

**概要**: 連絡先の詳細表示と編集を行う画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | 連絡先情報表示 | 氏名・メール・電話・会社名・メモ・グループを表示する |
| 2 | 連絡先編集 | 各フィールドを編集して保存できる（EDITOR 以上） |
| 3 | 連絡先削除 | 削除確認ダイアログ後に連絡先を削除できる（EDITOR 以上） |
| 4 | 閲覧のみモード | VIEWER は編集・削除ボタンが非表示または無効になる |

---

#### SCR-08 テンプレート一覧（`/dashboard/templates`）

**概要**: メールテンプレートを一覧表示・管理する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | テンプレート一覧表示 | 有効なテンプレートを更新日時の新しい順に表示する |
| 2 | アーカイブ済み切り替え | 有効一覧とアーカイブ済み一覧を切り替えて表示できる |
| 3 | 検索 | テンプレート名・件名でキーワード検索できる |
| 4 | 新規作成ボタン | テンプレート新規作成画面へ遷移するボタン（EDITOR 以上のみ有効） |
| 5 | 編集遷移 | 各テンプレートの編集画面へ遷移できる（EDITOR 以上） |
| 6 | メール作成遷移 | テンプレートからメール作成画面へ遷移できる（EDITOR 以上） |
| 7 | アーカイブ | テンプレートをアーカイブ（非表示）にできる（EDITOR 以上） |
| 8 | 復元 | アーカイブ済みテンプレートを有効に戻せる（EDITOR 以上） |

---

#### SCR-09 テンプレート新規作成（`/dashboard/templates/new`）

**概要**: 新しいメールテンプレートを作成する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | 入力フォーム | テンプレート名・件名・テキスト本文・HTML 本文を入力できる |
| 2 | 差し込み変数の使用 | `{{name}}` `{{companyName}}` などの変数を本文に記述できる |
| 3 | バリデーション | 名称・件名・本文の未入力エラーを表示する |
| 4 | 保存 | 保存後、テンプレート一覧へ戻る |

---

#### SCR-10 テンプレート編集（`/dashboard/templates/[id]/edit`）

**概要**: 既存のテンプレートを編集する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | 既存内容の読み込み | 現在の名称・件名・テキスト本文・HTML 本文が入力済みで表示される |
| 2 | 内容編集・保存 | 各フィールドを編集して上書き保存できる |
| 3 | アーカイブ | このテンプレートをアーカイブにできる |
| 4 | 完了フィードバック | 保存成功のメッセージを表示する |

---

#### SCR-11 配信履歴一覧（`/dashboard/campaigns`）

**概要**: 過去の配信キャンペーンを一覧表示する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | キャンペーン一覧表示 | 件名・テンプレート名・送信日時・ステータス・件数を一覧表示する |
| 2 | ステータスフィルター | ALL / DRAFT / SCHEDULED / SENDING / SENT / FAILED で絞り込める |
| 3 | 日付範囲フィルター | 送信日時の from / to で絞り込める |
| 4 | 作成者フィルター | 作成者 ID で絞り込める |
| 5 | 失敗優先表示 | FAILED を先頭に表示するオプションを切り替えられる |
| 6 | ページネーション | 1ページあたりの表示件数を選択できる（10〜200件） |
| 7 | 詳細遷移 | 各キャンペーンの詳細画面へ遷移できる |

---

#### SCR-12 配信履歴詳細（`/dashboard/campaigns/[id]`）

**概要**: 1回の配信の詳細情報を確認する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | キャンペーン情報表示 | 件名・テンプレート名・送信日時・ステータス・総件数を表示する |
| 2 | 件数サマリー | 成功・失敗・スキップ・合計件数を表示する |
| 3 | 宛先別結果一覧 | 各宛先のメールアドレス・ステータス・エラー内容を一覧表示する |
| 4 | 再送信 | 同じ宛先・内容でメール作成画面へ遷移できる（EDITOR 以上） |

---

#### SCR-13 メール作成・送信（`/dashboard/compose`）

**概要**: 選択した連絡先にメールを作成して送信する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | 宛先一覧表示 | 選択された連絡先と「送信可能」「スキップ対象」を区別して表示する |
| 2 | テンプレート選択 | 既存テンプレートを選択して件名・本文を自動入力できる |
| 3 | 件名・本文入力 | 件名・テキスト本文・HTML 本文を直接入力・編集できる |
| 4 | 差し込み変数表示 | 利用可能な変数（`{{name}}` 等）を参照できる |
| 5 | 予約送信 | 送信日時を指定して予約できる（空の場合は即時送信） |
| 6 | 送信ボタン | バリデーション後に送信処理を実行し、結果ページへ遷移する |

---

#### SCR-14 送信結果（`/dashboard/compose/result`）

**概要**: 送信処理の進捗と結果を表示する画面。

| # | 機能 | 詳細 |
|---|------|------|
| 1 | ステータス表示 | 現在のキャンペーンステータス（SENDING / SENT / FAILED 等）を表示する |
| 2 | 件数表示 | 送信成功・失敗・スキップの件数を表示する |
| 3 | 自動リフレッシュ | 送信中は一定時間ごとに画面を自動更新してリアルタイムに件数を反映する |
| 4 | 詳細リンク | 配信履歴詳細画面へのリンクを表示する |

---

### 2.3 画面遷移図

**未認証の場合**
- 未認証でダッシュボードにアクセス → SCR-01（ログイン画面）に自動リダイレクト

**ログイン後の遷移**

```
SCR-01 ログイン
  |
  | 認証成功
  v
SCR-02 ダッシュボードトップ
  |
  +-- [ユーザー管理] ---------> SCR-03 ユーザー管理（ADMIN のみ）
  |                             SCR-04 ユーザー招待
  |
  +-- [連絡先管理] -----------> SCR-05 連絡先一覧
  |                               |
  |                               +-- 新規作成 --> SCR-06
  |                               +-- 詳細・編集 -> SCR-07
  |                               |
  |                               | 連絡先を選択して「メール作成」
  |                               v
  |                             SCR-13 メール作成・送信
  |                               |
  |                               | 送信ボタン
  |                               v
  +-- [テンプレート管理] ------> SCR-08 テンプレート一覧   SCR-14 送信結果
  |                               |                            |
  |                               +-- 新規作成 --> SCR-09      | 履歴へ
  |                               +-- 編集 ------> SCR-10      v
  |                               |                       SCR-11 配信履歴一覧
  |                               | 「メール作成」              |
  |                               +---> SCR-13           SCR-12 配信履歴詳細
  |
  +-- [配信履歴] ------------> SCR-11 配信履歴一覧
                                 |
                                 +-- 詳細 ---------> SCR-12
                                 |
                                 | 「再送信」
                                 +---> SCR-13
```

---

## 3. データモデル概要

> **「データモデル」とは？**
> データベースにどんな情報を保存するかの設計図です。
> 「どのデータとどのデータが関係しているか」を示します。

### 3.1 エンティティ関連図

```
Organization（組織）
  |
  +-- Membership（メンバーシップ）--- role ---> ADMIN / EDITOR / VIEWER
  |         |
  |         +-- User（ユーザー）
  |               |
  |               +-- LoginToken（Magic Link トークン。有効期限 15 分）
  |
  +-- Invitation（招待。有効期限 72 時間）
  |
  +-- Contact（連絡先）
  |       groups フィールド --[JSON 配列]--> ContactGroup の ID を参照
  |
  +-- ContactGroup（グループ）
  |
  +-- EmailTemplate（テンプレート）
  |       isArchived: true でアーカイブ（物理削除しない）
  |
  +-- EmailCampaign（配信キャンペーン）
          subjectSnapshot  : 送信時の件名コピー
          textBodySnapshot : 送信時の本文コピー
          status           : DRAFT / SCHEDULED / SENDING / SENT / FAILED
          |
          +-- EmailCampaignRecipient（宛先明細）
                  emailSnapshot : 送信時のメールアドレスコピー
                  status        : PENDING / SENT / FAILED / SKIPPED
                  contactId     : Contact へのリンク（削除後は null）
```

### 3.2 主要エンティティの説明

| エンティティ | 現実のイメージ | 役割 |
|------------|-------------|------|
| Organization | 会社・チーム | テナント単位。全データはこれに紐づく |
| User | ログインするアカウント | メールアドレスで一意。複数の組織に所属可能 |
| Membership | 社員証 | User と Organization の紐づけ＋ロール情報 |
| Contact | 名刺 | 配信先の連絡先情報 |
| ContactGroup | 名刺のラベル | 連絡先のグループ分類 |
| EmailTemplate | メールのひな型 | 再利用可能なメールのフォーマット |
| EmailCampaign | 送信バッチ | 1回の配信単位。件名・本文のスナップショットを保持 |
| EmailCampaignRecipient | 送付状の控え | キャンペーンの宛先ごとの送信結果明細 |
| Invitation | 招待状 | ユーザー招待用のワンタイムトークン（72時間有効） |
| LoginToken | Magic Link の実体 | ワンタイムログイン用トークン（15分有効） |

> **「スナップショット」とは？**
> 送信時点の内容をそのままコピーして保存しておく仕組みです。
> テンプレートを後から編集しても、過去の送信内容は変わらず確認できます。

---

## 4. API 構成

> **「API」とは？**
> ブラウザから「このデータをください」「このデータを保存して」などの要求を受け取り、
> 処理して返す窓口のことです。

### 4.1 API グループ一覧

**認証系 API**

| メソッド | パス | 機能 |
|---------|------|------|
| POST | `/api/auth/login` | パスワードログイン |
| POST | `/api/auth/request-link` | Magic Link 送信 |
| GET | `/api/auth/verify` | Magic Link 検証 |
| POST | `/api/auth/logout` | ログアウト |
| POST | `/api/auth/password` | 自分のパスワード変更 |

**連絡先 API**

| メソッド | パス | 機能 | 必要ロール |
|---------|------|------|-----------|
| GET | `/api/contacts` | 連絡先一覧 | 認証済み |
| POST | `/api/contacts` | 連絡先作成 | EDITOR 以上 |
| GET | `/api/contacts/[id]` | 連絡先取得 | 認証済み |
| PUT | `/api/contacts/[id]` | 連絡先更新 | EDITOR 以上 |
| DELETE | `/api/contacts/[id]` | 連絡先削除 | EDITOR 以上 |
| GET | `/api/contacts/export` | CSV エクスポート | 認証済み |
| POST | `/api/contacts/import` | CSV インポート | EDITOR 以上 |

**グループ / 招待 / 管理者 API**

| メソッド | パス | 機能 | 必要ロール |
|---------|------|------|-----------|
| GET | `/api/groups` | グループ一覧 | 認証済み |
| POST | `/api/groups` | グループ作成 | EDITOR 以上 |
| DELETE | `/api/groups/[id]` | グループ削除 | EDITOR 以上 |
| POST | `/api/invitations/create` | 招待リンク発行 | EDITOR 以上 |
| GET | `/api/invitations/accept` | 招待受諾 | — |
| PATCH | `/api/admin/users/[id]/password` | 他ユーザーの PW 設定 | ADMIN |

### 4.2 レスポンス形式

全 API は JSON 形式で返す。

成功時:
```json
{ "ok": true, "データキー": { ... } }
```

失敗時:
```json
{ "ok": false, "error": "エラーコード文字列" }
```

---

## 5. 認証・認可設計

> **「認証」と「認可」の違い**
> - **認証（Authentication）**: 「あなたは誰ですか？」→ ログイン処理
> - **認可（Authorization）**: 「あなたはこれをしていいですか？」→ 権限チェック

### 5.1 認証方式

**パスワード認証**

1. メールアドレスとパスワードを入力
2. DB から passwordHash を取得
3. bcrypt でパスワードを照合
4. 一致した場合のみセッション発行 → ダッシュボードへ

**Magic Link 認証**

1. メールアドレスを入力
2. ランダムトークンを生成し、ハッシュ化して DB に保存（有効期限 15 分）
3. ターミナルに URL を出力
4. ユーザーが URL をブラウザで開く
5. トークンをハッシュ化して DB と照合。有効期限・使用済みチェック
6. セッション発行 → ダッシュボードへ

| 方式 | 概要 | 有効期限 |
|------|------|---------|
| パスワード認証 | メールアドレス＋パスワード（bcrypt ハッシュと照合） | — |
| Magic Link | ターミナルに表示されるワンタイム URL | 15 分 |

### 5.2 セッション管理

- ログイン成功後、`{ userId, organizationId, role }` を JSON 化して Cookie に保存
- Cookie は `HttpOnly: true`（JavaScript からアクセス不可）・`SameSite: lax` でセキュアに保持
- 以降のすべてのリクエストで Cookie を読み取ってセッションを検証

### 5.3 認可の流れ

1. セッションなし → **401 Unauthorized**（未認証）
2. VIEWER が変更操作をしようとした → **403 Forbidden**（権限不足）
3. 他組織のデータへのアクセス → **404 Not Found**
4. 通過 → 処理実行

---

## 6. メール送信設計

### 6.1 即時送信フロー

1. ユーザーが送信ボタンをクリック
2. EmailCampaign を DB に作成（status: `SENDING`）
3. EmailCampaignRecipient を宛先分まとめて作成
   - メールアドレスあり → `PENDING`
   - メールアドレスなし → `SKIPPED`
4. 結果ページにリダイレクト（3秒ごとに自動更新）
5. バックグラウンドで `processCampaign()` が各宛先にメール送信
   - 差し込み変数を置換してから送信
   - 成功 → `SENT`、失敗 → `FAILED`
6. 全件完了後、Campaign のカウントと status を更新

### 6.2 予約送信フロー

1. ユーザーが送信日時を指定して送信ボタンをクリック
2. EmailCampaign を作成（status: `SCHEDULED`、scheduledAt を保存）
3. 結果ページにリダイレクト（60秒ごとに自動更新）
4. `scheduler.ts` が 60 秒ごとにポーリング
5. scheduledAt を過ぎた SCHEDULED キャンペーンを検知 → status を `SENDING` に更新
6. `processCampaign()` でバックグラウンド送信処理

### 6.3 差し込み変数

| 変数 | 置換される値 | 例 |
|------|------------|-----|
| `{{name}}` | 連絡先の氏名 | 山田 太郎 |
| `{{companyName}}` | 連絡先の会社名 | 株式会社サンプル |
| `{{email}}` | 連絡先のメールアドレス | yamada@example.com |
| `{{phone}}` | 連絡先の電話番号 | 090-0000-0000 |

### 6.4 メール送信モード切り替え

環境変数 `EMAIL_MODE` で動作を切り替える。

| `EMAIL_MODE` | 動作 | 用途 |
|-------------|------|------|
| `console`（デフォルト） | ターミナルにメール内容を出力するだけ。実際には送信しない | 研修・開発環境 |
| `smtp` | SMTP サーバーに実際に送信する。別途 SMTP_HOST 等の設定が必要 | 本番環境 |

コンソール出力例:
```
=== EMAIL (console) ===
to: yamada@example.com
subject: 山田 太郎 様 〜ご案内〜
text: （本文）
=======================
```

---

## 7. データ保全・制約

| 項目 | 設計 | 理由 |
|------|------|------|
| 組織削除 | `onDelete: Cascade` で関連データをすべて削除 | 孤立データを残さない |
| ユーザー削除 | `onDelete: Restrict`（削除不可） | 過去の操作者情報を保全する |
| 連絡先削除 | 送信先明細の contactId は `SetNull`（履歴は保持） | 削除後も送信履歴が確認できる |
| テンプレート削除 | 物理削除でなく `isArchived: true` フラグで管理 | 過去の配信履歴との紐づけを保持 |
| メールアドレス重複 | 同組織内で一意制約。API 側で 409 を返す | 同じ相手への二重登録を防ぐ |
| グループ名重複 | 同組織内で一意制約。API 側でエラーを返す | 混乱を防ぐ |
